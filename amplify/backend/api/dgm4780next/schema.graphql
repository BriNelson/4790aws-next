type MoviesDB @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  director: String
  runtime: Int
  poster: String
  us_rating: String
  trailer: String
  trailer_thumbnail: String
  year: Int
  backdrop: String
  genre_names: [String]
  plot_overview: String
  sources: [String]
} 